import { styled } from ".";

export const Container = styled('div',{
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',

    minHeight: '100vh',

    rowGap: '2rem',
    padding: '2rem 0'
})